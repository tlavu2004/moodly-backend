meta {
  name: Mark habit done today
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/entries/today
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "habitId": "{{habitId}}",
    "done": true,
    "note": "30-minute run"
  }
}
