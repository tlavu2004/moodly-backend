meta {
  name: Expired token is unauthorized
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/habits
  body: none
  auth: bearer
}

auth:bearer {
  token: {{expiredAccessToken}}
}

tests {
  test("returns unauthorized for an expired access token", function() {
    expect(res.getStatus()).to.equal(401);
  });
}
